pipeline {
  agent any
  stages {
    stage("build") {
      steps {
         
          sh '''
            #!/bin/bash
            echo "hello world"
            echo "build"
         '''
        
          }
        }
      }
    }
