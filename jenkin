pipeline {
  agent any
  stages {
    stage("build") {
      steps {
         
          sh '''
            #!/bin/sh
            echo "hello world"
            echo "build"
            mkdir build
            cd build
            touch view.sh
            ls
            pwd
         
            
         '''
        
          }
          }
      stage ("test") {
        steps {
           Sh '''
           ls
            pwd
            echo stage test completed
            '''
        }
       
        }
      }
    }
